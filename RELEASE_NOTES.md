# LocalHub 2.2.4 RC3

这是基于 2.2.3 稳定播放内核整理出的第三个 2.2.4 发布候选版。RC3 不再增加播放器功能，只修正 RC2 暴露出的移动交互和刷新问题。

## RC3 新增调整

- **长按移动改为一次性手势**：在媒体卡片上按住约半秒进入移动，继续按住拖到左侧文件夹后松开即可完成；如果松手时不在目标文件夹上，则直接取消并退出移动状态。
- **不再需要 Esc**：鼠标 / 触控一旦松开就一定退出移动模式，不再保留“松开后再点目标文件夹”的二段式状态。
- **修复 RC2 点击冲突**：RC2 为了防止长按后的合成 click 误开视频，会短暂抑制点击；这与“松开后再点文件夹”的设计互相冲突。RC3 删除这条二段式路径，只保留按住拖放。
- **首页移动刷新修复**：文件移动成功后不再通过隐藏的“重新扫描”按钮间接刷新，而是直接调用索引重扫 API，完成后重新加载页面并恢复原来的首页 / 分类 / 搜索 / 页码位置，确保首页卡片、文件夹信息和本地真实路径一致。
- **真实文件移动回归测试**：新增自动测试，实际在临时媒体库中把文件从 A 文件夹移动到 B 文件夹，再重建 Catalog，验证旧路径消失、新路径进入首页索引。
- **顶部保持简洁**：右上角“移动位置”和“重新扫描”继续保持隐藏，内部能力仍然可用。

## 2.2.4 本次重点

- **播放器关闭链路**：退出播放器时会同步退出 Fullscreen，并取消当前兼容 FFmpeg 任务，减少残留黑屏和后台任务。
- **播放优先级**：播放视频时，缩略图和 Hover 预览必须让路；后台 FFmpeg 可被播放抢占，额外任务不会无限排队。
- **TS 兼容播放**：H.264/AAC TS 优先无损封装为 fragmented MP4，可在封装完成前提前开始播放；封装完成后自动切换到完整文件并恢复正常拖动。
- **视频处理状态**：旧的画面覆盖提示框已移除，兼容处理状态改为标题下方的轻量提示。
- **统一视频比例**：横屏、竖屏、4:3、1:1 以及带 DAR/SAR/rotation 元数据的视频全部使用同一套 Exact Fit 逻辑；宁可留黑边，也不裁掉源画面。
- **播放交互**：直接点击视频画面即可播放 / 暂停，普通窗口和全屏均有效。
- **本地推荐**：播放页恢复“猜你想看”，推荐算法只读取现有本地索引，不联网、不扫描媒体、不接管播放器 source。
- **推荐封面**：播放期间优先复用已有缓存 / Windows Explorer 共享缩略图，不绕过播放优先级启动高 I/O FFmpeg。
- **推荐页面布局**：推荐区改为自适应网格，不再使用窄小横向胶片条；播放器页面拥有自己的纵向滚动，底层媒体库页面保持固定。

## 稳定性原则

2.2.4 延续 2.2.3 的稳定播放器核心。推荐、缩略图、Hover、probe、兼容转换等辅助模块失败时，都不应该阻止普通 MP4 播放。

## 已知边界

- 超长 TS 虽然可以通过本地无损封装播放，但首次处理仍可能需要较长时间；2 小时以上 TS 的观看体验仍不如原生 MP4。后续计划提供独立的媒体库批量 TS → MP4 无损标准化工具，而不是继续增加播放器运行时复杂度。
- 浏览器原生播放能力仍取决于视频 / 音频编码；非浏览器友好编码可能需要转码。
- 推荐封面在视频正在播放且系统没有可复用缩略图缓存时，可能稍后或暂停播放后才补齐；这是为了保证视频播放优先。
- LocalHub 当前面向 Windows x64。
- 当前构建尚未使用商业代码签名证书，因此 Windows SmartScreen 仍可能提示“发布者未知”。

## RC3 建议回归测试

1. 普通 H.264 MP4 连续打开 / 关闭 20 次。
2. 全屏后关闭播放器，不残留黑色全屏壳。
3. 快速翻 15–20 页并触发大量 Hover 后，再打开普通 MP4，播放应仍正常。
4. 1080×1920 竖屏视频普通窗口 / 全屏均完整显示。
5. 1906×1080 等横屏视频与 PotPlayer 同帧比较，内容边界一致，只允许黑边不同。
6. 小型 H.264/AAC TS 可提前起播；封装完成后进度条恢复任意拖动。
7. 推荐区有封面时正常显示，无封面时也不能影响播放。
8. 在播放器内滚鼠标滚轮，只滚播放器详情页，不推动底层媒体库首页。
9. 首页和“全部视频”都可以长按约半秒后直接拖到左侧文件夹，松手完成移动。
10. 长按后如果没有拖到目标文件夹，松手立即取消，不需要 Esc，也不会误打开视频。
11. 移动成功后页面自动刷新并尽量恢复原视图 / 页码；检查卡片显示的新文件夹与 Windows 本地文件真实位置一致。

---

# LocalHub 2.2.3

这是 LocalHub 第一个正式公开发行版本。

LocalHub 把一个普通本地媒体文件夹变成浏览器里的私人媒体站：视频、图包、文件夹、Tag、评分、收藏、继续观看、快速改名和拖动分类都集中在一个本地界面里完成。

## 本版重点

- Windows 单文件 `LocalHub.exe`，无需安装 Python 或 Node.js。
- 首页保持 13–15 个视频，不再把整个媒体库堆进浏览器。
- 文件夹导航、全部视频、图包 / 图册、搜索、收藏与继续观看。
- 多图片目录自动折叠为图包；混合目录保留视频并把图片合并成图包。
- 视频列表不挂 `<video>` 流，缩略图与悬停预览按需获取，避免大量磁盘 I/O。
- 6 帧渐进式悬停预览，全局只处理一个视频。
- 原生播放 + 自动兼容播放；AVI、MPG、TS、部分 MOV/MKV 等会按需 remux 或转码，不修改原文件。
- 自定义播放器时间轴、倍速、音量、全屏、继续观看。
- Tag 在卡片和播放页直接原地增删，不弹编辑窗口。
- 1–5 分个人评分，低调显示，用于自己判断内容质量。
- 播放页标题双击即可改名，保留扩展名并恢复播放位置。
- “移动位置”用于分类整理：卡片可拖到左侧目标文件夹，也可点选后一键移动。
- 改名 / 移动后 Tag、评分、收藏和观看进度会跟随迁移。
- 图片阅读器按原图比例显示；超长图自动按宽度滚动阅读。
- 默认只监听 `127.0.0.1`，媒体不会上传互联网。
- 启动器显式绕过系统代理访问 localhost，兼容代理 / VPN 环境。
- Windows GUI 无控制台黑框，托盘菜单可重新打开媒体库或退出。

## 安装

1. 下载 `LocalHub.exe`。
2. 把它放到你的媒体总目录。
3. 双击运行。

LocalHub 会在同目录创建 `.localhub/` 保存 Tag、评分、目录索引和运行状态；不会修改媒体内容本身，只有你主动执行改名或移动时才会操作真实文件。
