# LocalHub 2.4.0

这是当前 LocalHub 主线的稳定冻结版本。

2.4.0 在 2.2.3 的基础上继续强化本地媒体浏览、播放器稳定性、文件夹整理与本地智能能力。这个 Release 的目的不是继续扩大功能范围，而是为当前已经完成的主线代码建立一个明确、可下载、可回退的稳定节点。

## 本版重点

- Windows 单文件 `LocalHub.exe`，无需安装 Python、Node.js 或 FFmpeg。
- 保持大媒体库低 I/O 浏览：分页、按需缩略图和渐进式悬停预览。
- 持续加强播放器稳定性，媒体探测采用 fail-fast / non-blocking 策略，避免后台 probe 阻塞正式播放。
- 视频、图包、文件夹、搜索、收藏与继续观看保持统一的本地媒体体验。
- Tag、1–5 分评分、原地改名和拖动分类继续作为核心整理能力。
- 改名 / 移动后相关本地元数据继续跟随迁移。
- 本地推荐、视觉索引与 Auto Tag 实验能力已经进入代码库，但 AI Tag 尚未完成真实媒体库效果验证，因此本版不把它作为成熟能力承诺。
- 默认仅监听 `127.0.0.1`，不需要账号，不上传媒体。
- 完整 Windows CI 会执行 Python / JavaScript 校验、浏览器 smoke、播放器稳定性、Auto Tag 低风险 smoke、媒体兼容、元数据、推荐性能、单 EXE 打包和打包后自检。

## 安装

1. 下载 `LocalHub.exe`。
2. 把它放到你的媒体总目录。
3. 双击运行。

LocalHub 会在媒体目录创建 `.localhub/` 保存本地元数据、索引、兼容播放缓存和运行状态。

## 文件安全说明

改名和移动会操作真实文件。LocalHub 会阻止同名覆盖并迁移相关元数据，但 2.4.0 尚未提供完整的文件操作历史 / Undo 机制。

因此在下一阶段中，文件操作 Journal、移动 / 改名历史以及 `Ctrl+Z` 撤销将作为重要的商业级安全能力继续推进。

## AI Tag 状态

2.4.0 已包含本地视觉索引和基于用户已有 Tag 的建议机制，以及相应的 smoke test。

当前测试分支额外修复旧版 sparse Tag 设置迁移与自动 Tag 落库：旧 `version=2` 设置也会补齐专业内置词库，每个内置 Tag 组至少 20 个候选；已存在的视觉索引会直接重新分类并自动写入高置信 Tag，不要求逐个视频人工确认。

但当前只代表工程链路已经接入，并不代表推荐准确率已经经过真实用户或大型真实媒体库验证。后续将通过真实样本测试准确率、误报率、性能和播放期间的 I/O 影响，再决定它在正式产品中的定位。

## Windows SmartScreen

当前构建尚未使用商业代码签名证书，因此 Windows SmartScreen 可能显示“发布者未知”。发行包同时提供 `SHA256.txt` 用于校验文件完整性。

## 已知边界

- LocalHub 目前主要面向 Windows x64。
- 浏览器原生播放能力仍取决于实际容器和音视频编码。
- 部分媒体可能需要本地兼容处理。
- 文件移动 / 改名目前没有完整 Undo 历史。
- AI Tag 尚处于真实效果验证前阶段。

---

**LocalHub 2.4.0 是当前主线的稳定冻结点。后续开发将在此基础上继续推进，而不是修改这个 Release。**
