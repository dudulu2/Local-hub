# Timeline Repair Experiment

Manual A/B test only. The normal RC4 player remains the default. `修复播放` fully decodes the selected video, rebuilds a constant-frame-rate timeline, re-encodes H.264/AAC to a temporary MP4, and switches playback to that repaired copy without modifying the source file.
